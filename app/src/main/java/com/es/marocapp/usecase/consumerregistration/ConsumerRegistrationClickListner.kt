package com.es.marocapp.usecase.consumerregistration

import android.view.View

interface ConsumerRegistrationClickListner {
    fun onSubmitClickListner(view : View)
    fun onCalenderCalenderClick(view : View)
    fun onGenderSelectionClick(view : View)
}